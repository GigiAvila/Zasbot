import styled from 'styled-components'

export const CompanyLogoWrapper = styled.div`
  width: 10vw;
  height: auto;
  display: flex;
  margin: 0 2vw;

  > img {
    width: 100%;
    height: auto;
  }
`
